import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'earth.aslanbb.simplifylife',
  appName: 'simplify-life',
  webDir: 'build',
  server: {
    androidScheme: 'https'
  }
};

export default config;
